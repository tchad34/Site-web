self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch(e){data={body:event.data?event.data.text():'Nouvelle activite sur NoxStream.'}}const title=data.title||'NoxStream';const options={body:data.body||'Nouvelle creation de compte.',icon:data.icon||'/avatars/avatar-noxstream.svg',badge:data.badge||'/avatars/avatar-noxstream.svg',data:data.data||{url:'/admin/'},tag:'noxstream-account-created',renotify:true};event.waitUntil(self.registration.showNotification(title,options));});self.addEventListener('notificationclick',event=>{event.notification.close();const url=(event.notification.data&&event.notification.data.url)||'/admin/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus' in c){c.navigate(url);return c.focus();}}return clients.openWindow(url);}));});